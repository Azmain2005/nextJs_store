/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["cdn.ishop.cholobangla.com","fakestoreapi.com", "i.postimg.cc"], // add any external domains you use
  },
};

export default nextConfig;
